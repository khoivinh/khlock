# 2026-04-20 Devlog — Happyhour rebrand (Tier 1) + enhancements + bug batch

## What changed

Executed **Tier 1** of the Happyhour rebrand + enhancement plan (`.claude/plans/i-managed-to-mellow-church.md`). Source spec: `docs/2026-04-20-khlock-edits.md`. Tier 1 is the autonomous slice — no Figma assets or external dashboard actions required.

### 1. Rebrand plumbing (strings + storage keys)

| File | Change |
|---|---|
| `package.json` | `"name": "khlock"` → `"happyhour"` |
| `api/package.json` | `"name": "khlock-api"` → `"happyhour-api"` |
| `api/wrangler.jsonc` | Worker name renamed; `happyhour.day` added to CORS allowlist (kept `khlock.pages.dev` as transitional fallback). D1 database name kept as `khlock-db` per plan (internal, disruptive to rename). |
| `client/index.html` | `<title>Khlock</title>` → `<title>Happyhour</title>` |
| `client/src/pages/world-clock.tsx` | Header text `Khlock` → `Happyhour` (placeholder; Tier 2 will swap in the Figma logo treatment). LocalStorage keys renamed to `world-happyhour-*`. |
| `client/src/components/time-zone-converter.tsx` | `STORAGE_KEY` → `world-happyhour-zones` |
| `client/src/hooks/use-cloud-sync.ts` | Sync snapshot + timestamp keys → `world-happyhour-*` |
| `client/src/lib/theme-provider.tsx` | Theme storage key → `world-happyhour-theme` |

**Data decision:** fresh start, no migration. Existing users lose local tiles on next visit and re-initialize to defaults (Tokyo / New York / Paris). D1 cloud-synced data is unaffected — the binding still points at `khlock-db`.

### 2. UX enhancements

- **Scroll-to-current-city on edit** (`digital-clock.tsx`): Added a `useEffect` that runs when the city popover opens, finds the selected `CommandItem` by its `data-testid`, and calls `scrollIntoView({ block: "center" })`. Also guarded the filter list so the currently-selected city is always present even when truncated to 100 results, otherwise scroll-to would silently fail for cities outside the default slice (there are 30k+ cities in the dataset).
- **Native `confirm()` replaced with shadcn Dialog** (`digital-clock.tsx`): The browser's native `confirm("Remove X?")` was rendering as `khlock.pages.dev says: Remove newYork_US?` — now renders as a shadcn `<Dialog>` titled "Happyhour" with `Cancel` / `Remove` actions. ESC + outside-click close via Radix defaults.
- **Badge typography** (`digital-clock.tsx`, desktop + mobile): Next / Prev / Offset badges moved from `text-[7px] font-bold leading-[15px]` to `text-[8px] font-semibold leading-[16px]`. Added `tracking-[0.2px]` to the Next/Prev spans only (per spec the offset badge has no tracking).
- **Clock tile removal animation** (`digital-clock.tsx`): Introduced an `isRemoving` state; when the user confirms removal the tile gets `animate-out fade-out-0 zoom-out-95 duration-200`, then a 200ms `setTimeout` calls the parent's `onRemove` to actually remove the tile from the list. Drops the previous abrupt disappearance.

### 3. Bug fixes

- **Z-index on multi-word city (`Los Angeles`, etc.)**: Bumped the city-selector `PopoverContent` className from implicit `z-50` to `z-[60]` so it always sits above the active-edit tile's yellow-highlight state.
- **Default logged-out second tile showing "Select City"**: Code already specified `["tokyo_JP", "newYorkCity_US", "paris_FR"]`. The bug was stale `newYork_US` in localStorage — the fresh `world-happyhour-*` key prefix resolves it automatically. No code change needed.
- **Idle data-clearing**: Added `visibilitychange` + `focus` listeners on `TimeZoneConverter` that force-refresh `currentTime` when the tab returns to the foreground. Standalone `useEffect` so it doesn't tear down with drag activity or custom-mode toggles.
- **Closest city targets Los Angeles for Northern CA**: Added `client/src/lib/closest-city.ts` with a haversine-distance search over all ~30k cities. On mount, after setting the timezone-derived fallback, calls `navigator.geolocation.getCurrentPosition` and upgrades `heroZone` to the geographically-nearest city. Cached for 24h in `world-happyhour-local-city`. Silent — denial/timeout stays on the old timezone heuristic, so no regression for users who deny permission.
- **Drawer toggle alignment (open vs closed)**: Removed `mt-[2px]` from the sidebar's internal close button so it aligns with the page-header toggle. Both `DrawerToggleIcon` SVGs already share the same viewBox + rect coords; the misalignment came from this one offset class. Tier 2 will swap in the redrawn (thicker-stroke) icons from Figma but the geometry stays consistent.

## Verification

- `npx tsc --noEmit` — clean (exit 0)
- `npm run build` — clean build, `happyhour@1.0.0` header visible in output
- Manual browser verification **not yet done** — user to smoke-test `npm run dev` and confirm.

## What's next

- **Tier 3** (needs user action): Cloudflare Pages custom-domain setup for `happyhour.day`, DNS at registrar, GitHub repo rename, optional Clerk instance swap. Delivered as a chat walkthrough after Tier 2 merges.

---

## Tier 2 — Figma-assisted asset work (landed same day)

Pulled all referenced Figma nodes via authenticated MCP and used the matching production-ready SVGs already checked in at `design/User Interface Designs/` and `design/Brand/`.

### Changes

| File | Change |
|---|---|
| `client/public/favicon.svg` (new) | Black rounded square + yellow smiley-clock logo (copied from `design/User Interface Designs/favicon.svg`, 512×512) |
| `client/public/favicon.png` (removed) | Old Khlock rainbow PNG deleted — modern browsers handle SVG favicons natively, so the PNG fallback was only serving the old branding |
| `client/public/og.png` (new) | 1200×630 resized from `design/User Interface Designs/happyhour-opengraph.png` via `sips`, 94 KB |
| `client/index.html` | Added `og:*`, `twitter:*`, and `apple-touch-icon` meta tags; `og:image` points at `https://happyhour.day/og.png` (absolute URL required by scrapers); description refreshed to "The indispensable world clock. It's always that time somewhere." (matches OG copy) |
| `client/src/components/icons/drawer-closed.tsx` (new) | Inline React SVG, fills replace strokes — matches Figma node 95-2575 closed state |
| `client/src/components/icons/drawer-open.tsx` (new) | Inline React SVG with right-pointing arrow — matches Figma 95-2575 open state |
| `client/src/components/icons/happyhour-logo.tsx` (new) | Yellow smiley with checkmark eye sweep (derived from favicon SVG, without the rounded square background); face features use `currentColor` so they adapt to the theme's foreground |
| `client/src/components/sidebar.tsx` | `DrawerToggleIcon` now just delegates to the new components; close button wrapper bumped from `w-[24px]` → `w-[25px]` to match the new SVG viewBox |
| `client/src/pages/world-clock.tsx` | Added `<HappyhourLogo>` alongside the "Happyhour" wordmark in the header; sized as `w-[0.95em]` so it scales automatically with the existing font-size animation |
| `client/src/index.css` | Added `.happy` palette — `#ffd900` background (HSL `51 100% 50%`), near-black foreground, white cards for contrast, dark sidebar so controls stay legible |
| `client/src/lib/theme-provider.tsx` | Extended `Theme` union + `ResolvedTheme` to include `"happy"`; class-list cleanup now strips all three theme classes before applying the new one |
| `client/src/components/sidebar.tsx` | `cycleTheme` rotation now `system → light → dark → happy → system`; Happy icon is the new smiley logo; label "Happy" |
| `client/src/hooks/use-cloud-sync.ts` + `client/src/lib/api.ts` + `client/src/pages/world-clock.tsx` | Widened `SyncablePreferences.theme` / `CloudPreferences.theme` unions to include `"happy"` |
| `api/src/routes/preferences.ts` | Added `"happy"` to `VALID_THEMES` so the backend accepts it over the sync endpoint |
| `client/src/components/theme-toggle.tsx` (deleted) | Unused component — was the old 3-way toggle, not rendered anywhere. Removed to avoid drift with sidebar's 4-way cycle |
| `client/src/components/digital-clock.tsx` | Removal animation duration bumped from 200ms → 800ms per feedback; swapped `duration-[800ms]` → `[animation-duration:800ms]` to silence a Tailwind ambiguity warning |

### Notes

- Brand color tokens pulled from Figma: Background Yellow `#ffd900`, Logo Mark `#ffcd05`. Both referenced in the new favicon SVG and happyhour-logo component.
- Happy Mode intentionally uses a dark sidebar (same as light mode's dark sidebar) rather than a yellow one — the sidebar's interactive density makes yellow-on-yellow hard to read.
- The scroll-shrink header animation (`world-clock.tsx:42-64`) is untouched; the logo inherits scale via `em`-based sizing, so it shrinks in lock-step with the wordmark.

## Verification (Tier 2)

- `npx tsc --noEmit` — clean
- `npm run build` — clean (final bundle after fix: no warnings, 495 KB main chunk)
- Dev server HMR — all Tier 2 updates applied without reload

## Open for user to verify

- Browser tab favicon (should show black rounded square with yellow smiley)
- OG preview (paste `http://localhost:5173` into a local preview tool; real preview won't work until `happyhour.day` is live since we used absolute URLs)
- Cycle through Appearance — `System → Light → Dark → Happy → System`
- Happy Mode yellow contrast — eyeball the weather colors, tile hover states, and destructive-red Remove button against yellow bg; if anything reads poorly, we tune the palette

## Notes for future sessions (Tier 1)

- Clock tile z-index fix is pragmatic (raise popover to `z-[60]`) rather than diagnostic. If multi-word-city breakage recurs, check whether the sortable wrapper's `transform` is creating an unexpected stacking context when the tile is in edit mode.
- Drawer alignment fix is also pragmatic. If the user reports it's still off by a few pixels, check the `topOffset` calculation in `world-clock.tsx:55–58` against the sidebar's `top: topOffset - 18px` + `pt-[18px]` math — the close button should land at exactly `topOffset.top` with no mt.
